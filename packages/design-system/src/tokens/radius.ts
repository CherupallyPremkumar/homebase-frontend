/**
 * Border radius — 3 levels only.
 * Cards, buttons, inputs all use 'md'.
 * Full round only for avatars and badges.
 */
export const radius = {
  sm: '4px',
  md: '8px',
  lg: '12px',
  full: '9999px',
} as const;
